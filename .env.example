UNIFI_API_KEY=your-unifi-api-key
CLOUDFLARE_API_TOKEN=your-cloudflare-api-token
DOMAIN=example.com
# Optional: subdomain prefix, e.g. "unifi" → hostname.unifi.example.com
# SUBDOMAIN=
# Optional: cron schedule (default: every 5 minutes)
# CRON=*/5 * * * *
